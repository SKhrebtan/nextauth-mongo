import connectMongo from '../back/connectMongo';
const User = require('../back/models/user');

// export default async function loginUser(req, res) {
//       await connectMongo()
//       //  await User.create(req)
//      }